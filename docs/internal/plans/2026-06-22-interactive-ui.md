# Interactive UI Refactor Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `llm-switch switch` 和 `llm-switch save` 的菜单从"输入数字"改为"箭头键选择 + 回车确认"，用 `@inquirer/prompts` 实现，并美化 `list` 输出。

**Architecture:** 重写 `src/ui.ts` 内部使用 `@inquirer/prompts`，保持公开接口不变（`pickProfile` / `promptAlias`）。`commands/*.ts` 零改动。新增 `promptNewAlias` 供 `promptAlias` 链式调用。`commands/list.ts` 仅修改输出字符串。测试用 `vi.mock('@inquirer/prompts')` 拦截。

**Tech Stack:** TypeScript · `@inquirer/prompts` · vitest `vi.mock`

---

## 文件结构

```
packages/cli/
├── src/
│   ├── ui.ts                       # 重写：内部用 @inquirer/prompts
│   └── commands/
│       └── list.ts                # 微调输出字符串
├── test/
│   ├── ui.test.ts                  # 重写：用 vi.mock
│   └── commands/
│       └── save.test.ts            # 移除旧的 "prompts for alias interactively" 测试
└── package.json                    # 添加 @inquirer/prompts 到 dependencies
```

每个 src 文件单一职责；测试与源码一一对应。

---

## Task 1: 添加 @inquirer/prompts 依赖

**Files:**
- Modify: `packages/cli/package.json` (add dependency)
- Modify: `pnpm-lock.yaml` (regenerated by pnpm install)

- [ ] **Step 1: 编辑 packages/cli/package.json**

在 `dependencies` 中添加 `"@inquirer/prompts": "^7.0.0"`：

```json
  "dependencies": {
    "@inquirer/prompts": "^7.0.0",
    "commander": "^12.0.0",
    "picocolors": "^1.1.0",
    "zod": "^3.23.0"
  },
```

- [ ] **Step 2: 安装依赖**

```bash
cd /Users/xavier/Projects/Github/llm-switch
pnpm install
```

预期：lockfile 更新，无错误。

- [ ] **Step 3: 验证类型可用**

```bash
cd /Users/xavier/Projects/Github/llm-switch/packages/cli
pnpm typecheck
```

预期：无错误（虽然还没用，imports 应该是合法的）。

- [ ] **Step 4: 验证测试仍绿**

```bash
pnpm test
```

预期：85/85 仍通过（新依赖不影响旧代码）。

- [ ] **Step 5: 提交**

```bash
cd /Users/xavier/Projects/Github/llm-switch
git add packages/cli/package.json pnpm-lock.yaml
git commit -m "feat(cli): add @inquirer/prompts dependency"
```

---

## Task 2: 重写 test/ui.test.ts（用 vi.mock）

**Files:**
- Modify: `packages/cli/test/ui.test.ts`（完全重写）

- [ ] **Step 1: 写新的测试文件**

完全替换 `/Users/xavier/Projects/Github/llm-switch/packages/cli/test/ui.test.ts`：

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

vi.mock('@inquirer/prompts', () => ({
  select: vi.fn(),
  input: vi.fn(),
  isCancel: (v: unknown) => typeof v === 'symbol',
}));

import { select, input } from '@inquirer/prompts';
import { pickProfile, promptAlias, promptNewAlias } from '../src/ui.js';
import { UserCancelledError } from '../src/errors.js';
import type { Profile } from '../src/scanner.js';

const mockSelect = vi.mocked(select);
const mockInput = vi.mocked(input);

describe('pickProfile', () => {
  const profiles: Profile[] = [
    { alias: 'glm', path: '/p/glm', active: false },
    { alias: 'kimi', path: '/p/kimi', active: true },
  ];

  let savedIsTTY: boolean | undefined;

  beforeEach(() => {
    savedIsTTY = process.stdout.isTTY;
    Object.defineProperty(process.stdout, 'isTTY', { value: true, configurable: true });
  });

  afterEach(() => {
    Object.defineProperty(process.stdout, 'isTTY', { value: savedIsTTY, configurable: true });
    vi.clearAllMocks();
  });

  it('returns selected profile', async () => {
    mockSelect.mockResolvedValueOnce(profiles[0]!);
    const result = await pickProfile(profiles);
    expect(result?.alias).toBe('glm');
  });

  it('returns null on cancel', async () => {
    mockSelect.mockResolvedValueOnce(Symbol('cancel') as never);
    const result = await pickProfile(profiles);
    expect(result).toBeNull();
  });

  it('pre-selects active profile via default option', async () => {
    mockSelect.mockResolvedValueOnce(profiles[1]!);
    await pickProfile(profiles);
    expect(mockSelect).toHaveBeenCalledWith(
      expect.objectContaining({
        default: profiles[1],
      }),
    );
  });

  it('returns null for empty profiles without calling select', async () => {
    const result = await pickProfile([]);
    expect(result).toBeNull();
    expect(mockSelect).not.toHaveBeenCalled();
  });

  it('throws UserCancelledError when no TTY', async () => {
    Object.defineProperty(process.stdout, 'isTTY', { value: false, configurable: true });
    await expect(pickProfile(profiles)).rejects.toBeInstanceOf(UserCancelledError);
    expect(mockSelect).not.toHaveBeenCalled();
  });
});

describe('promptAlias', () => {
  let savedIsTTY: boolean | undefined;

  beforeEach(() => {
    savedIsTTY = process.stdout.isTTY;
    Object.defineProperty(process.stdout, 'isTTY', { value: true, configurable: true });
  });

  afterEach(() => {
    Object.defineProperty(process.stdout, 'isTTY', { value: savedIsTTY, configurable: true });
    vi.clearAllMocks();
  });

  it('returns existing alias when selected', async () => {
    mockSelect.mockResolvedValueOnce('glm');
    const result = await promptAlias(['glm', 'kimi']);
    expect(result).toBe('glm');
  });

  it('falls through to promptNewAlias on "+ Create new"', async () => {
    mockSelect.mockResolvedValueOnce(Symbol('new') as never);
    mockInput.mockResolvedValueOnce('newalias');
    const result = await promptAlias(['glm']);
    expect(mockInput).toHaveBeenCalled();
    expect(result).toBe('newalias');
  });

  it('skips select and calls input directly when existing is empty', async () => {
    mockInput.mockResolvedValueOnce('first');
    const result = await promptAlias([]);
    expect(mockSelect).not.toHaveBeenCalled();
    expect(result).toBe('first');
  });

  it('returns null on cancel at select', async () => {
    mockSelect.mockResolvedValueOnce(Symbol('cancel') as never);
    const result = await promptAlias(['glm']);
    expect(result).toBeNull();
  });

  it('returns null on cancel at input (after "+ Create new")', async () => {
    mockSelect.mockResolvedValueOnce(Symbol('new') as never);
    mockInput.mockResolvedValueOnce(Symbol('cancel') as never);
    const result = await promptAlias(['glm']);
    expect(result).toBeNull();
  });

  it('throws UserCancelledError when no TTY', async () => {
    Object.defineProperty(process.stdout, 'isTTY', { value: false, configurable: true });
    await expect(promptAlias(['glm'])).rejects.toBeInstanceOf(UserCancelledError);
  });
});

describe('promptNewAlias', () => {
  let savedIsTTY: boolean | undefined;

  beforeEach(() => {
    savedIsTTY = process.stdout.isTTY;
    Object.defineProperty(process.stdout, 'isTTY', { value: true, configurable: true });
  });

  afterEach(() => {
    Object.defineProperty(process.stdout, 'isTTY', { value: savedIsTTY, configurable: true });
    vi.clearAllMocks();
  });

  it('returns trimmed alias', async () => {
    mockInput.mockResolvedValueOnce('  myalias  ');
    const result = await promptNewAlias([]);
    expect(result).toBe('myalias');
  });

  it('passes validate function that rejects empty input', async () => {
    mockInput.mockResolvedValueOnce('valid');
    await promptNewAlias([]);
    const call = mockInput.mock.calls[0]![0] as { validate?: (v: string) => boolean | string };
    expect(call.validate!('')).toBe('Required');
  });

  it('passes validate function that rejects invalid format', async () => {
    mockInput.mockResolvedValueOnce('valid');
    await promptNewAlias([]);
    const call = mockInput.mock.calls[0]![0] as { validate?: (v: string) => boolean | string };
    expect(call.validate!('BAD!')).toMatch(/Must match/);
  });

  it('passes validate function that rejects duplicates', async () => {
    mockInput.mockResolvedValueOnce('valid');
    await promptNewAlias(['glm']);
    const call = mockInput.mock.calls[0]![0] as { validate?: (v: string) => boolean | string };
    expect(call.validate!('glm')).toMatch(/already exists/);
  });

  it('accepts valid new alias', async () => {
    mockInput.mockResolvedValueOnce('newalias');
    await promptNewAlias(['glm']);
    const call = mockInput.mock.calls[0]![0] as { validate?: (v: string) => boolean | string };
    expect(call.validate!('newalias')).toBe(true);
  });

  it('returns null on cancel', async () => {
    mockInput.mockResolvedValueOnce(Symbol('cancel') as never);
    const result = await promptNewAlias([]);
    expect(result).toBeNull();
  });

  it('throws UserCancelledError when no TTY', async () => {
    Object.defineProperty(process.stdout, 'isTTY', { value: false, configurable: true });
    await expect(promptNewAlias([])).rejects.toBeInstanceOf(UserCancelledError);
  });
});
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
cd /Users/xavier/Projects/Github/llm-switch/packages/cli
pnpm test test/ui.test.ts
```

预期：FAIL，错误为 `pickProfile is not a function`（因为 ui.ts 还是旧实现）。

- [ ] **Step 3: 不提交（实现来后再一起提交）**

---

## Task 3: 重写 src/ui.ts

**Files:**
- Modify: `packages/cli/src/ui.ts`（完全重写）

- [ ] **Step 1: 写新的 ui.ts**

完全替换 `/Users/xavier/Projects/Github/llm-switch/packages/cli/src/ui.ts`：

```ts
import { select, input, isCancel } from '@inquirer/prompts';
import type { Readable, Writable } from 'node:stream';
import type { Profile } from './scanner.js';
import { ALIAS_RE } from './config.js';
import { UserCancelledError } from './errors.js';

export interface ReadlineIO {
  input: Readable;
  output: Writable;
}

function ensureTTY(): void {
  if (!process.stdout.isTTY) {
    throw new UserCancelledError(
      'Interactive mode requires a TTY. Use: llm-switch <alias>',
    );
  }
}

export async function pickProfile(
  profiles: Profile[],
  _io?: ReadlineIO,
): Promise<Profile | null> {
  ensureTTY();
  if (profiles.length === 0) return null;

  const active = profiles.find((p) => p.active);
  const result = await select<Profile>({
    message: 'Select profile to switch to:',
    choices: profiles.map((p) => ({
      name: p.alias,
      value: p,
    })),
    default: active,
  });

  if (isCancel(result)) return null;
  return result;
}

export async function promptAlias(
  existing: string[],
  _io?: ReadlineIO,
): Promise<string | null> {
  ensureTTY();

  if (existing.length === 0) {
    return promptNewAlias(existing);
  }

  const NEW_SENTINEL = Symbol('__create_new');
  const result = await select<string | symbol>({
    message: 'Choose a profile name:',
    choices: [
      ...existing.map((name) => ({ name, value: name })),
      { name: '+ Create new', value: NEW_SENTINEL },
    ],
  });

  if (isCancel(result)) return null;
  if (result === NEW_SENTINEL) {
    return promptNewAlias(existing);
  }
  return result;
}

export async function promptNewAlias(
  existing: string[],
  _io?: ReadlineIO,
): Promise<string | null> {
  ensureTTY();

  const result = await input({
    message: 'New alias name:',
    validate: (value: string) => {
      const trimmed = value.trim();
      if (!trimmed) return 'Required';
      if (!ALIAS_RE.test(trimmed)) {
        return `Must match ${ALIAS_RE} (lowercase, digits, . _ -, 1-64 chars)`;
      }
      if (existing.includes(trimmed)) {
        return `Alias '${trimmed}' already exists`;
      }
      return true;
    },
  });

  if (isCancel(result)) return null;
  return result.trim();
}
```

- [ ] **Step 2: 运行 ui.test.ts**

```bash
cd /Users/xavier/Projects/Github/llm-switch/packages/cli
pnpm test test/ui.test.ts
```

预期：所有测试 PASS（pickProfile 5 个 + promptAlias 6 个 + promptNewAlias 7 个 = 18 个）。

- [ ] **Step 3: 提交**

```bash
cd /Users/xavier/Projects/Github/llm-switch
git add packages/cli/src/ui.ts packages/cli/test/ui.test.ts
git commit -m "feat(cli): rewrite ui with @inquirer/prompts for keyboard navigation"
```

---

## Task 4: 微调 commands/list.ts 输出格式

**Files:**
- Modify: `packages/cli/src/commands/list.ts`

- [ ] **Step 1: 替换输出字符串**

完全替换 `/Users/xavier/Projects/Github/llm-switch/packages/cli/src/commands/list.ts`：

```ts
import type { ConfigDir } from '../config.js';
import { getConfigDir } from '../config.js';
import { listProfiles } from '../scanner.js';
import { NoProfilesError } from '../errors.js';

export interface CommandIO {
  stdout: { write(s: string): unknown };
}

export async function run(io: CommandIO): Promise<void> {
  const configDir: ConfigDir = getConfigDir();
  const profiles = await listProfiles(configDir);

  if (profiles.length === 0) {
    throw new NoProfilesError(
      "No profiles found. Create one with: llm-switch save <alias>",
    );
  }

  const maxAliasLen = Math.max(...profiles.map((p) => p.alias.length));

  const lines = ['Available profiles:', ''];
  profiles.forEach((p) => {
    const marker = p.active ? '●' : '○';
    const tag = p.active ? ' (active)' : '';
    const padded = p.alias.padEnd(maxAliasLen);
    lines.push(`  ${marker} ${padded}${tag}  ${p.path}`);
  });
  lines.push('');
  lines.push('Use `llm-switch switch` to change active profile.');
  io.stdout.write(lines.join('\n') + '\n');
}
```

- [ ] **Step 2: 运行 list 测试**

```bash
cd /Users/xavier/Projects/Github/llm-switch/packages/cli
pnpm test test/commands/list.test.ts
```

预期：2/2 PASS（输出格式变化不影响 NoProfilesError 行为，但 list 包含特定字段的测试需要更新——见 Task 5）。

- [ ] **Step 3: 提交**

```bash
cd /Users/xavier/Projects/Github/llm-switch
git add packages/cli/src/commands/list.ts
git commit -m "feat(cli): beautify list output with bullet markers and padding"
```

---

## Task 5: 调整 commands/save.test.ts（移除旧的交互测试）

**Files:**
- Modify: `packages/cli/test/commands/save.test.ts`

- [ ] **Step 1: 替换 save.test.ts**

完全替换 `/Users/xavier/Projects/Github/llm-switch/packages/cli/test/commands/save.test.ts`：

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import fs from 'node:fs/promises';
import path from 'node:path';
import os from 'node:os';
import { Readable } from 'node:stream';
import { run } from '../../src/commands/save.js';
import { NoCurrentSettingsError, InvalidAliasError } from '../../src/errors.js';

let tmpDir: string;
let savedEnv: string | undefined;

beforeEach(async () => {
  tmpDir = await fs.mkdtemp(path.join(os.tmpdir(), 'llm-switch-test-'));
  savedEnv = process.env.CLAUDE_CONFIG_DIR;
  process.env.CLAUDE_CONFIG_DIR = tmpDir;
});

afterEach(async () => {
  if (savedEnv === undefined) delete process.env.CLAUDE_CONFIG_DIR;
  else process.env.CLAUDE_CONFIG_DIR = savedEnv;
  await fs.rm(tmpDir, { recursive: true, force: true });
  vi.restoreAllMocks();
});

function mockIO() {
  const writes: string[] = [];
  return {
    writes,
    stdin: Readable.from(['']),
    stdout: { write: (s: string) => writes.push(s) },
    stderr: { write: (s: string) => writes.push(s) },
  };
}

describe('save command', () => {
  it('throws NoCurrentSettingsError when settings.json missing', async () => {
    const io = mockIO();
    await expect(run({ alias: 'glm', ...io, isTTY: true } as never)).rejects.toBeInstanceOf(NoCurrentSettingsError);
  });

  it('throws InvalidAliasError for bad alias', async () => {
    await fs.writeFile(path.join(tmpDir, 'settings.json'), '{}');
    const io = mockIO();
    await expect(run({ alias: 'BAD!', ...io, isTTY: true } as never)).rejects.toBeInstanceOf(InvalidAliasError);
  });

  it('saves current settings to profile path', async () => {
    await fs.writeFile(path.join(tmpDir, 'settings.json'), '{"a":1}');
    const io = mockIO();

    await run({ alias: 'glm', ...io, isTTY: true } as never);

    expect(JSON.parse(await fs.readFile(path.join(tmpDir, 'settings.json.glm'), 'utf8'))).toEqual({ a: 1 });
  });

  it('overwrites existing profile (current is truth)', async () => {
    await fs.writeFile(path.join(tmpDir, 'settings.json'), '{"new":true}');
    await fs.writeFile(path.join(tmpDir, 'settings.json.glm'), '{"old":true}');
    const io = mockIO();

    await run({ alias: 'glm', ...io, isTTY: true } as never);

    expect(JSON.parse(await fs.readFile(path.join(tmpDir, 'settings.json.glm'), 'utf8'))).toEqual({ new: true });
    expect(io.writes.join('')).toContain('Overwrote');
  });
});
```

注意：移除了 `prompts for alias interactively when not provided` 测试（因为交互逻辑现在在 ui.test.ts 中用 vi.mock 测试，命令文件不应再测 UI 行为）。

- [ ] **Step 2: 运行 save 测试**

```bash
cd /Users/xavier/Projects/Github/llm-switch/packages/cli
pnpm test test/commands/save.test.ts
```

预期：4/4 PASS。

- [ ] **Step 3: 提交**

```bash
cd /Users/xavier/Projects/Github/llm-switch
git add packages/cli/test/commands/save.test.ts
git commit -m "test(cli): remove obsolete readline-based interactive test"
```

---

## Task 6: 调整 commands/switch.test.ts（如需要）

**Files:**
- Modify: `packages/cli/test/commands/switch.test.ts`

- [ ] **Step 1: 检查现有 switch.test.ts 是否需要修改**

```bash
cat /Users/xavier/Projects/Github/llm-switch/packages/cli/test/commands/switch.test.ts
```

- [ ] **Step 2: 运行 switch 测试看是否通过**

```bash
cd /Users/xavier/Projects/Github/llm-switch/packages/cli
pnpm test test/commands/switch.test.ts
```

预期：4/4 PASS（不传 alias + isTTY: false 的测试仍抛 UserCancelledError，新 ui.ts 行为一致）。

如果通过：无需修改，跳到 Step 4。

如果有失败的测试：根据错误信息调整。最可能的失败点：`isTTY: false` 的测试——如果新实现用 `process.stdout.isTTY` 而非 io.isTTY，可能行为不同。当前设计 `ensureTTY()` 检查 `process.stdout.isTTY`，所以传 io.isTTY: false 不影响 ensureTTY 行为——但因为 process.stdout.isTTY 在测试中是 undefined（不是 false），ensureTTY 不会抛错，会真正尝试调 inquirer → mock 未设 → 报错。

**修复方法**：在 ensureTTY 函数前 stub process.stdout.isTTY，或在测试中明确设置。

如果需要修复，替换 `test/commands/switch.test.ts` 中的 `throws UserCancelledError when interactive menu cancelled (no TTY)` 测试为：

```ts
  it('throws UserCancelledError when interactive menu cancelled (no TTY)', async () => {
    await fs.writeFile(path.join(tmpDir, 'settings.json.glm'), '{}');
    const io = mockIO('\n');

    const savedIsTTY = process.stdout.isTTY;
    Object.defineProperty(process.stdout, 'isTTY', { value: false, configurable: true });
    try {
      await expect(run({ alias: undefined, ...io, isTTY: false } as never)).rejects.toBeInstanceOf(UserCancelledError);
    } finally {
      Object.defineProperty(process.stdout, 'isTTY', { value: savedIsTTY, configurable: true });
    }
  });
```

并修改文件顶部的 import 添加 `vi`：

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
```

并在 `afterEach` 中加 `vi.restoreAllMocks()`。

- [ ] **Step 3: 提交（如有修改）**

```bash
cd /Users/xavier/Projects/Github/llm-switch
git add packages/cli/test/commands/switch.test.ts
git commit -m "test(cli): stub process.stdout.isTTY in switch e2e tests"
```

---

## Task 7: 完整测试 + 类型检查 + 构建

**Files:** 无新增

- [ ] **Step 1: 完整测试**

```bash
cd /Users/xavier/Projects/Github/llm-switch
pnpm test
```

预期：所有测试 PASS（应 >= 85 + 新增测试）。

- [ ] **Step 2: 类型检查**

```bash
cd /Users/xavier/Projects/Github/llm-switch/packages/cli
pnpm typecheck
```

预期：无错误。

- [ ] **Step 3: 构建**

```bash
pnpm build
```

预期：`packages/cli/dist/cli.js` 生成。

- [ ] **Step 4: 手动 smoke test（list）**

```bash
TMPDIR=$(mktemp -d)
echo '{"env":{"ANTHROPIC_BASE_URL":"https://x"}}' > "$TMPDIR/settings.json"
echo '{}' > "$TMPDIR/settings.json.glm"
CLAUDE_CONFIG_DIR="$TMPDIR" node /Users/xavier/Projects/Github/llm-switch/packages/cli/bin/llm-switch.js list
```

预期输出：
```
Available profiles:

  ● glm   (active)   .../settings.json.glm

Use `llm-switch switch` to change active profile.
```

- [ ] **Step 5: 手动 smoke test（switch 走非交互路径）**

```bash
CLAUDE_CONFIG_DIR="$TMPDIR" node /Users/xavier/Projects/Github/llm-switch/packages/cli/bin/llm-switch.js switch glm
CLAUDE_CONFIG_DIR="$TMPDIR" node /Users/xavier/Projects/Github/llm-switch/packages/cli/bin/llm-switch.js list
```

预期：`switch` 直接走非交互路径（参数已传），不调起 inquirer；`list` 显示 glm 仍 active。

- [ ] **Step 6: 清理**

```bash
rm -rf "$TMPDIR"
```

- [ ] **Step 7: git status 检查**

```bash
cd /Users/xavier/Projects/Github/llm-switch
git status
git log --oneline -10
```

预期：working tree clean，6 个新 commit 在最近 10 条中。

---

## Task 8: 版本 bump + 发布

**Files:**
- Modify: `packages/cli/package.json` (version 0.1.0 → 0.2.0)
- Modify: `CHANGELOG.md` (add 0.2.0 entry)

- [ ] **Step 1: 修改 package.json version**

将 `packages/cli/package.json` 中 `"version": "0.1.0"` 改为 `"version": "0.2.0"`。

- [ ] **Step 2: 更新 CHANGELOG.md**

在 `/Users/xavier/Projects/Github/llm-switch/CHANGELOG.md` 的 `## [Unreleased]` 区块下添加：

```markdown
## [0.2.0] - 2026-06-22

### Changed
- Interactive menus (`switch`, `save`) now use arrow-key navigation via `@inquirer/prompts` instead of numbered typing
- `switch` menu pre-selects the currently active profile
- `save` menu shows existing aliases + "+ Create new" option
- `list` output reformatted with bullet markers (`●` / `○`) and column padding

### Added
- `promptNewAlias` exported from `ui.ts` for chained calls from `promptAlias`

### Dependencies
- Added `@inquirer/prompts` ^7.0.0
```

并将 `## [Unreleased]` 改为空（删除内容）。

- [ ] **Step 3: 提交**

```bash
cd /Users/xavier/Projects/Github/llm-switch
git add packages/cli/package.json CHANGELOG.md
git commit -m "chore: bump version to 0.2.0"
```

- [ ] **Step 4: push main + tag + 发布**

```bash
cd /Users/xavier/Projects/Github/llm-switch
git push origin main

git tag -d v0.2.0 2>/dev/null
git tag v0.2.0
git push origin :refs/tags/v0.2.0 2>/dev/null
git push origin v0.2.0 --force
```

预期：CI 自动跑测试 → 通过 → publish job 触发 → npm publish。

- [ ] **Step 5: 验证 npm 发布**

```bash
sleep 90
gh api 'repos/xavier2code/llm-switch/actions/runs?per_page=3' --jq '.workflow_runs[] | "\(.head_branch) \(.status) \(.conclusion)"' 2>&1
```

预期：v0.2.0 完成 success。

```bash
npm view llm-switch@0.2.0 version
```

预期：`0.2.0`。

---

## 完成标准

- ✅ 8 个 Task 全部 checkbox 勾选
- ✅ `pnpm test` 通过（含 ui.test.ts 18 个测试）
- ✅ `pnpm build` 产物可运行
- ✅ npm 上有 `llm-switch@0.2.0`
- ✅ working tree clean
- ✅ tag v0.2.0 在 GitHub 上

可手动验证：用 `npm i -g llm-switch@0.2.0` 后运行 `llm-switch switch` 体验箭头菜单。